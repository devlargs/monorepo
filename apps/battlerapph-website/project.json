{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/battlerapph-website",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/next:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "root": "apps/battlerapph-website",
        "outputPath": "dist/apps/battlerapph-website"
      },
      "configurations": {
        "development": {
          "outputPath": "apps/battlerapph-website"
        },
        "production": {}
      }
    },
    "serve": {
      "executor": "@nrwl/next:server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "battlerapph-website:build",
        "dev": true
      },
      "configurations": {
        "development": {
          "buildTarget": "battlerapph-website:build:development",
          "dev": true
        },
        "production": {
          "buildTarget": "battlerapph-website:build:production",
          "dev": false
        }
      }
    },
    "export": {
      "executor": "@nrwl/next:export",
      "options": {
        "buildTarget": "battlerapph-website:build:production"
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["coverage/apps/battlerapph-website"],
      "options": {
        "jestConfig": "apps/battlerapph-website/jest.config.ts",
        "passWithNoTests": true
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/battlerapph-website/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}
